export type Lesson = {
  id: number;
  title: string;
  category: string;
  description: string;
};

export type SalahStep = {
  id: number;
  name: string;
  description: string;
  arabic: string;
  translation: string;
  imageUrl: string;
  audioUrl: string;
};
